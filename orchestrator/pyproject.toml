[project]
name = "moufida-orchestrator"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "redis>=5.0",
    "psycopg[binary]>=3.1",
    "affinitree",
]
