server {
    listen 3002;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all routes resolve to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
